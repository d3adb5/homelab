apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: transmission-movies-oauth2-proxy
spec:
  forwardAuth:
    address: http://transmission-movies-oauth2-proxy.media.svc.cluster.local
    trustForwardHeader: true
