apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: sonarr-oauth2-proxy
spec:
  forwardAuth:
    address: http://sonarr-oauth2-proxy.media.svc.cluster.local
    trustForwardHeader: true
