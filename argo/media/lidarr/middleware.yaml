apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: lidarr-oauth2-proxy
spec:
  forwardAuth:
    address: http://lidarr-oauth2-proxy.media.svc.cluster.local
    trustForwardHeader: true
