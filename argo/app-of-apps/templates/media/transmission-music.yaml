apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: transmission-music
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: media

  sources:
    - repoURL: {{ .Values.repository.homelab.url }}
      targetRevision: {{ .Values.repository.homelab.targetRevision }}
      path: argo/media/transmission-music

    - repoURL: https://oauth2-proxy.github.io/manifests
      targetRevision: 10.7.0
      chart: oauth2-proxy

      helm:
        valuesObject:
          config:
            existingSecret: transmission-music-oidc-secrets

            configFile: |-
              email_domains = ["*"]
              provider = "keycloak-oidc"
              oidc_issuer_url = "https://id.d3adb5.ca/realms/core"
              redirect_url = "https://music.transmission.d3adb5.ca/oauth2/callback"
              code_challenge_method = "S256"
              allowed_groups = ["/Transmission"]
              upstreams = [ "static://202" ]
              reverse_proxy = true
              skip_provider_button = true

          metrics:
            serviceMonitor:
              enabled: true

          ingress:
            enabled: true
            className: traefik
            hosts:
              - "music.transmission.d3adb5.ca"
            path: /oauth2
            tls:
              - hosts:
                  - "music.transmission.d3adb5.ca"
                secretName: transmission-music-tls

    - repoURL: https://charts.d3adb5.net
      targetRevision: 0.3.0
      chart: transmission

      helm:
        valuesObject:
          transmission:
            generateCredentials: false
          image:
            repository: lscr.io/linuxserver/transmission
            pullPolicy: IfNotPresent
            tag: "4.1.3@sha256:0913501f5d05c019f5e3555a3299df1d8de420fdf882c92a8857787f9fbf999d"
          replicaCount: 1
          resources:
            requests:
              cpu: 350m
              memory: 2Gi
            limits:
              memory: 2Gi
          persistence:
            config:
              storageClass: longhorn
          ingress:
            enabled: true
            className: traefik
            host: music.transmission.d3adb5.ca
            tls:
              enabled: true
              secretName: transmission-music-tls
            annotations:
              cert-manager.io/cluster-issuer: letsencrypt
              traefik.ingress.kubernetes.io/router.middlewares: "{{ .Values.media.namespace }}-transmission-music-oauth2-proxy@kubernetescrd"
          peerService:
            type: LoadBalancer
            port: 32555
            externalTrafficPolicy: Local
            annotations:
              metallb.io/loadBalancerIPs: "192.168.1.38"
          extraEnv:
            - name: PEERPORT
              value: "32555"
            - name: TZ
              value: America/Vancouver
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
          extraVolumes:
            - name: music-library
              persistentVolumeClaim:
                claimName: {{ .Values.media.libraries.music.claimName }}
          extraVolumeMounts:
            - name: music-library
              mountPath: /downloads
          affinity:
            podAntiAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values: [ transmission ]
                  topologyKey: kubernetes.io/hostname

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.media.namespace }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true

  ignoreDifferences:
    - kind: Service
      jqPathExpressions:
        - .spec.ports[]?.nodePort
