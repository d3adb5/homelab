apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: longhorn-oauth2-proxy
spec:
  forwardAuth:
    address: http://longhorn-oauth2-proxy.longhorn-system.svc.cluster.local
    trustForwardHeader: true
